# Week 7 Day 2

# What I did

Today we added a query that returns the fields I want as a dict. Then I built a prompt to return a JSON structured output. 

This is important because the pipeline needs to store the result, qurey it, and put it in a report. The structured data is better than a paragraph of prose and that structured data is easier for the program to do what it needs to do with the data. 

At first, I my code was failing but then I asked claude and fixed the bugs for me.

# Prose vs structured output

A pipeline needs JSON instead of a paragraph because a paragraph is good when a person is trying to read it but for the pipeline, it needs to store the result, qurey it, and put it in a report.

# My prediction

My prediction was that it was going to answer the questions like it did yesterday, but I was wrong.

It responded with a clean JSON that didn't have any usless information, just everything that I needed to see.

# Fields to questions

The canonical tag is the "offical" URL for the page. This is showing if the US page treats itself as the canonical home or if points somewhere else. 

This is import for brand research because this is showing us if it's a sign of thin or non-localized US presence.

# What I don't fully understand

I am still a bit confused on these past 2 weeks. I don't think I like doing the data stuff that much. It is a lot diff from scraping pages and the coding for building the prompts and SQL files are really confusing compared to coding for scraping.


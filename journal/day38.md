# Week 8 Day 4

# What I did

I started by testing `render_brief`. After making sure the test works, I made a `requirements.txt` and a `README.md` file for my repository.

The `requrements.txt` shows all the required libraries needed for this project and the `README.md` is a brief summary of what this project is for and what the code is doing.

# Why render_brief was easy to test

Testing `render_brief` was easier than testing `fetch_one` because `render_brief` is a pure function. It doesn't need to call from anywhere or fetch from anywhere. No internet needed, no api key needed just a pure function that has no side effects.

# Substring assertions

Im not too sure about this answer but I think it is because if it were to print the exact document out with each output, it could break.

# The README test

I believe if someone where to clone this repo, they would be able to reproduce this project only using the README. It has ever step to get this project running and all they need to do is figure out how to get their api key.

This is also knowing that this person knows how to use a terminal and a text editing application.

# What I don't fully understand

The thing I did't fully understand this lesson was Substring assertions. I don't really understand how it is working and it didn't really explain that well, just a brief explanation as to why it is being used. 

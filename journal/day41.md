# Week 9 Day 2

# What I did

I started by creating a template for the index part of the website and added a query to `db.py` that lists the brands. 

# Relative links

A relative link is telling your code to look at a specific file not at all the files to find what you need. 

Im not too sure on why all the pages have to live in `site/`.

# HTML vs CSS - the split

You can honestly change everything about the website with just `style.css` you just can't change the content of data without changing the template around. 

# The two elses

In `brief.html` the `else` is used when `if` can't be executed but in `index.html` the `else` is used as a loop in the database so even if the database is empty, it will print "No brands yet" instead of a blank list.

# What I don't fully understand

I don't really understand the differences in the `else` for `brief.html` and `index.html`. To  me, it seems like they are basically doing the same thing. 